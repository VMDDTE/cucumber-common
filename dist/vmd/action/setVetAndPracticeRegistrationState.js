"use strict";

/**
 * Set whether or not the vet is registered with the practice
 *
 *
 * @param  {String}   registeredState    (is|is not)
 */
module.exports = function (registeredState) {
  if (registeredState === 'is') {// Make sure the vet is registered with the practice
  } else {// Make sure the vet is NOT registered with the practice
    }
};