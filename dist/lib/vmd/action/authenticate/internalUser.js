"use strict";

var _require = require('cucumber'),
    Given = _require.Given;

var runLoginPage = require('./run-login-page');

Given(/^the internal user is logged in$/, function () {//to be implemented
});
Given(/^the public user is logged in as '([^']*)?'$/, function (role) {//to be implemented
});